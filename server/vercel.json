{
  "version": 2,
  "name": "pepta-api",
  "buildCommand": "rm -rf node_modules/.prisma && prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
